# Phase 0 — status

`✅ done` · `🟡 scaffolded, needs a live run` · `⬜ needs you (external / manual)`

Everything that is code or config has been written and, where runnable offline,
verified. The remaining work needs Docker + wp-env, live competitor sites, or a
GitHub repo with Actions — none available in the environment this was scaffolded
from.

## W1 — Teardown and tokens

### 0.1 Competitive teardown

| # | Task | State | Notes |
| --- | --- | :-: | --- |
| 1.1 | Repo + ADR log | ✅ | `git init` done. `docs/decisions/` with template + `0001`–`0006`. License header decided (ADR-0002), applied to all PHP. |
| 1.2 | Benchmark six competitor demo sites | ⬜ | Needs Lighthouse runs against live demo sites. Procedure + empty table in `docs/benchmarks.md`. |
| 1.3 | Benchmark the floor | ⬜ | Needs a local TT25 + Woo install. Same table. |
| 1.4 | `docs/benchmarks.md` | 🟡 | File written with full methodology; rows are placeholders until 1.2/1.3 run. |
| 1.5 | Feature frequency audit | 🟡 | `docs/feature-audit.md` — 30-row matrix ready; ✓/✗ cells need the demo-site audit. |
| 1.6 | Reconcile against v1.0 ledger | ⬜ | Reconciliation table + omission-ADR slot in place; needs 1.5 scores and the v1.0 ledger. |

### 0.2 Design token system

| # | Task | State | Notes |
| --- | --- | :-: | --- |
| 2.1 | `tokens.json` | ✅ | `packages/tokens/tokens.json` — appendix shape, verbatim. |
| 2.2 | Responsive contract | ✅ | `docs/responsive-token-contract.md`; encoded as `responsiveCategories` / `fixedCategories` in generated `defaults.ts`. |
| 2.3 | Token compiler | ✅ | `packages/tokens/src/compile.mjs` — emits `theme.settings.json`, `tokens.css`, `defaults.ts`, and merges `themes/flagship/theme.json`. Runs as `pnpm run tokens:build`. |
| 2.4 | Snapshot-test the compiler | ✅ | Generated files committed; `tokens:check` fails on drift; `packages/tokens/test/compile.test.mjs` (7 tests) passes. |

## W2 — Monorepo and tooling

### 0.3 Scaffold

| # | Task | State | Notes |
| --- | --- | :-: | --- |
| 3.1 | pnpm workspace | ✅ | Root `package.json`, `pnpm-workspace.yaml` (`themes/* plugins/* packages/* tools/*`), `.npmrc` `hoist=false`. |
| 3.2 | Composer + PSR-4 | ✅ | Root `composer.json`, namespace per unit, `woocommerce-stubs` + `phpstan-wordpress` as dev deps. |
| 3.3 | wp-env config | 🟡 | `.wp-env.json` is the appendix config + `tools/seed`. "wp-admin in < 2 min cold" needs Docker to verify. |
| 3.4 | TypeScript + lint | ✅ | `tsconfig.base.json` (`strict`), `tsconfig.json` project refs, `.eslintrc.cjs` (`@wordpress/eslint-plugin`), `.prettierrc.cjs` (`@wordpress/prettier-config`). Separate `lint` and `typecheck` scripts. |
| 3.5 | PHPCS ruleset | ✅ | `phpcs.xml.dist` — WordPress ruleset + PHPCompatibilityWP `8.2-`, text domain `ledger`, prefixes `ledger`/`Ledger`/`LEDGER`. |
| 3.6 | PHPStan level 8 | ✅ | `phpstan.neon.dist` — appendix config, level 8, no baseline (ADR-0004). Run needs `composer install`. |

### 0.4 Skeletons that boot

| # | Task | State | Notes |
| --- | --- | :-: | --- |
| 4.1 | Three plugin skeletons | ✅ | `core`, `blocks`, `commerce` — header, `Container`, activation/deactivation, graceful `Requirements` checks, `uninstall.php` cleanup. `core` provides the shared container via `ledger_container`. |
| 4.2 | Theme skeleton | ✅ | `themes/flagship` — `theme.json` generated by the compiler, `templates/` (index, archive-product, single-product), `parts/`, `functions.php` = enqueue + register only. |
| 4.3 | Verify graceful degradation | 🟡 | Templates use core + WooCommerce blocks so the shop stays browsable with all Ledger plugins off. Needs a wp-env run to confirm. |

## W3 — CI gate and seed data

### 0.5 Seed data

| # | Task | State | Notes |
| --- | --- | :-: | --- |
| 5.1 | Seed generator | ✅ | `wp ledger seed generate` — 5,000 products, 40% variable (3 attributes, ~12 variations), 60% simple, realistic titles/prices/stock/categories/reviews. Deterministic seed. |
| 5.2 | Real images | 🟡 | `Image_Factory` draws real JPEGs with GD (no placeholder service) and forces every registered size. `--images` flag. Needs a run. |
| 5.3 | Restorable snapshot | 🟡 | `seed:export` / `seed:restore` scripts (SQL + media tarball, `< 30s` restore). Snapshot binaries are gitignored — commit `shop.sql` via LFS after first run. |
| 5.4 | Reset procedure | ✅ | `pnpm run reset` (`tools/seed/reset.mjs`) + `docs/reset-procedure.md`. |

### 0.6 The pipeline

| # | Task | State | Notes |
| --- | --- | :-: | --- |
| 6.1 | Static analysis job | ✅ | `.github/workflows/ci.yml` → `static`: PHPCS, PHPStan, ESLint, `tsc` as distinct steps + token check/tests. |
| 6.2 | Playwright harness | ✅ | `playwright.config.ts` (targets `:8889`), `tests/e2e/no-jquery.spec.ts` (appendix spec), `tests/e2e/smoke.spec.ts` (shop/product/cart/account, no console errors). |
| 6.3 | Lighthouse CI + assertions | ✅ | `lighthouserc.js` — appendix config verbatim. `tools/seed/lhci-url.mjs` swaps `SEED_ID` for a real product ID in CI. Run needs wp-env. |
| 6.4 | Bundle size limits | 🟡 | Budgets (60 KB CSS / 40 KB JS, per-entry table in `docs/performance-budget.md`) are defined but `size-limit` / `.size-limit.json` / the `perf` `size` step were removed until there is a bundle to weigh — see `docs/phase-2-checklist.md`. Lighthouse `resource-summary` transfer caps still enforce the totals. |
| 6.5 | Branch protection | ⬜ | GitHub setting. Push the repo, then require every check on `main`, no direct pushes. |
| 6.6 | Prove the gate fails | ⬜ | Commit a 200 KB junk stylesheet + a 400 ms main-thread blocker, watch CI go red on the byte + TBT assertions, screenshot, revert. Must be done once. |
| 6.7 | `docs/performance-budget.md` | ✅ | Written — numbers, enforcement layer per metric, and the never-relaxed rule (ADR-0006). |

## Offline verification done

- `node packages/tokens/src/compile.mjs` → 4 artefacts written
- `node packages/tokens/src/compile.mjs --check` → in sync
- `node --test packages/tokens/test/**/*.test.mjs` → 7/7 pass
- `node themes/flagship/build.mjs` → `build/style.css` (597 B), `assets/tokens.css` (1.4 KB)

## Next actions for you

1. `pnpm install && composer install` — resolve the pinned toolchain, then run
   `pnpm run lint`, `pnpm run typecheck`, `composer run phpcs`, `composer run phpstan`
   and fix whatever the real linters flag (the code is written to their rules
   but has not been run through them here).
2. `pnpm wp-env start` — confirm wp-admin in < 2 min (3.3), then check the shop
   browses with all three plugins deactivated (4.3).
3. Run the benchmarks (1.2, 1.3) and fill `docs/benchmarks.md`; do the feature
   audit (1.5) and reconcile (1.6).
4. Generate + export the seed snapshot (5.1–5.3), commit `shop.sql` via LFS.
5. Push to a private GitHub repo, enable branch protection (6.5), then do the
   deliberate gate-failure test (6.6).
6. Re-estimate Phase 1 using how long the above actually took as the velocity
   baseline.
